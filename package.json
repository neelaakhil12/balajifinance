{
  "name": "balaji-savings-and-finance",
  "version": "1.0.0",
  "description": "Dynamic Chit Fund & Savings Management Application for Balaji Savings & Finance",
  "private": true,
  "scripts": {
    "dev": "npm --prefix client run dev",
    "server": "npm --prefix server start",
    "build": "cd client && npm install && npm run build",
    "start": "cd server && node index.js"
  }
}
